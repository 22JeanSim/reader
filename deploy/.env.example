# 复制为 .env 后填真实值 (.env 不入 git)
# 宿主发布端口(容器内固定 8080); 不设默认 8080, 无需反代直连即可用
# 注册邀请码 (secure 模式下注册必填)
READER_APP_INVITECODE=change-me-invite
# 管理密码 (secure 模式开关钥匙)
READER_APP_SECUREKEY=change-me-adminpwd

# READER_PORT=8080
